import type {Metadata} from 'next'; import {GalleryView} from '@/components/gallery-view'; import {activities} from '@/lib/content'; import {resolveActivityMedia} from '@/lib/media-resolver';
export const metadata:Metadata={title:'Toute la galerie',description:'Galerie des médias et de l’identité visuelle de DOROPO DRAPEAU.'};
const media=activities.flatMap(a=>resolveActivityMedia(a.mediaPrefix,a.mediaCount).map((m,i)=>({id:`${a.id}-${i}`,src:m.src,type:m.type,alt:`${a.title} — média ${i+1}`})));
export default function Page(){return <main><section className="section page-hero"><div className="container"><div className="eyebrow">Médias</div><h1 className="title">Toute la galerie</h1><p className="lead">Une galerie interactive préparée pour accueillir progressivement les photos et vidéos des activités. Les deux variantes du logo actuellement disponibles sont déjà présentées.</p></div></section><section className="section"><div className="container"><GalleryView media={media}/></div></section></main>}