import type {Metadata} from 'next'; import {members} from '@/lib/members'; import {resolveMemberMedia} from '@/lib/media-resolver'; import {ClickableMedia} from '@/components/lightbox';
export const metadata:Metadata={title:'Nos membres actuels',description:'Présentation des membres retenus pour la composition actuelle de DOROPO DRAPEAU.'};
export default function Page(){return <main><section className="section page-hero"><div className="container"><div className="eyebrow">Notre équipe</div><h1 className="title">Nos membres actuels</h1><p className="lead">Cette présentation reprend les personnes et fonctions retenues dans le document interne de référence fourni pour cette rubrique.</p></div></section><section className="section"><div className="container grid cards-3 member-grid">{members.map(m=>{const media=resolveMemberMedia(m.photo);return <article className="card member-card" key={m.id}><div className="member-photo">{media?<ClickableMedia src={media.src} alt={`Photo de ${m.name}`} width={400} height={400}/>:<div className="media-missing" aria-hidden="true"/>}</div><div className="eyebrow">{m.role}</div><h2>{m.name}</h2><p className="muted">{m.bio}</p></article>})}</div></section></main>}
