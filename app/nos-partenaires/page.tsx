import type {Metadata} from 'next'; import {partners} from '@/lib/content'; import {resolvePartnerMedia} from '@/lib/media-resolver'; import {ClickableMedia} from '@/components/lightbox';
export const metadata:Metadata={title:'Nos partenaires',description:'Partenaires de DOROPO DRAPEAU et acteurs qui accompagnent ses initiatives.'};
export default function Page(){return <main><section className="section page-hero"><div className="container"><div className="eyebrow">Notre réseau</div><h1 className="title">Nos partenaires</h1><p className="lead">DOROPO DRAPEAU s’appuie sur des relations locales et des collaborations destinées à renforcer ses initiatives communautaires, éducatives, culturelles et sociales.</p></div></section><section className="section"><div className="container grid cards-3">{partners.map(p=>{const m=resolvePartnerMedia(p.file);return <article className="card partner-card" key={p.name}><div className="partner-media">{m?<ClickableMedia src={m.src} alt={`Visuel de ${p.name}`} width={600} height={400}/>:<div className="media-missing" aria-hidden="true"/>}</div><div className="eyebrow">Partenaire</div><h2>{p.name}</h2><p className="muted">{p.description}</p></article>})}</div></section></main>}
