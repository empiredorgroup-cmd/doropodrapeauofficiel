'use client';
import Link from 'next/link'; import Image from 'next/image'; import {Menu,X} from 'lucide-react'; import {useEffect,useState} from 'react'; import {usePathname} from 'next/navigation'; import {THEME_LOGO,themeForPath} from '@/lib/theme';
const links=[['Accueil','/'],['Le DD parleur','/dd-parleur'],['Nos domaines','/nos-domaines'],['Nos activités','/nos-activites'],['Nos membres','/nos-membres'],['Qui sommes-nous ?','/qui-sommes-nous'],['Nos partenaires','/nos-partenaires'],['Nous rejoindre','/nous-rejoindre'],['Nous soutenir','/nous-soutenir'],['Nos bilans','/bilans'],['Galerie','/galerie'],['Contacts','/contacts']];
export function SiteHeader(){const [open,setOpen]=useState(false);const pathname=usePathname();const theme=themeForPath(pathname);useEffect(()=>{document.body.classList.toggle('menu-open',open);return()=>document.body.classList.remove('menu-open')},[open]);useEffect(()=>{document.body.dataset.theme=theme},[theme]);return <header className="site-header"><div className="nav-wrap"><Link href="/" className="brand" onClick={()=>setOpen(false)}><Image src={THEME_LOGO[theme]} alt="DOROPO DRAPEAU" width={58} height={58}/><span>DOROPO<br/><b>DRAPEAU</b></span></Link><button className="menu-btn" aria-label={open?'Fermer le menu':'Ouvrir le menu'} aria-expanded={open} onClick={()=>setOpen((v:boolean)=>!v)}>{open?<X/>:<Menu/>}</button><nav className={open?'nav open':'nav'} aria-label="Navigation principale">{links.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</nav></div></header>}
